# Tracked shared defaults for the definitive web app.
#
# This file is committed for non-secret shared defaults only.
# Real provider credentials live outside the repo in the VPS secret env file.

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.minimax.io/v1
OPENAI_MODEL=MiniMax-M2.7
APP_ENABLE_REAL_LLM=true
APP_ENABLE_DEBUG_LOGS=false
APP_DEMO_USER_ID=demo-lerner
